import { BorderShadowDirective } from './border-shadow.directive';

describe('BorderShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderShadowDirective();
    expect(directive).toBeTruthy();
  });
});
